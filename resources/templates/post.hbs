{{> header}}
  <section class="hero is-medium is-primary is-bold">
    <div class="hero-body">
      <div class="container">
        <h1 class="title">
          {{title}}
        </h1>
        <h2 class="subtitle">
          {{date}}
        </h2>
      </div>
    </div>
  </section>


<div class="column is-half is-offset-one-quarter">
{{{text}}}
</div>

{{> footer}}
